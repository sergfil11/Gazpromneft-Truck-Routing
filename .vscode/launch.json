{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch prog.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/prog.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "PATH",
                    "value": "C:\\gurobi1203\\bin;${env:PATH}"
                }
            ]
        }
    ]
}